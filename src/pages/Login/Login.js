import React from 'react';
import { Header, LoginForm } from '../../components/';

const Login = () => {
    return (
        <Header linkMain="/">
            <LoginForm />
        </Header>
    );
};

export default Login;
