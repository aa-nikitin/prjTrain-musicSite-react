import React from 'react';
import { Header, LoginForm } from '../../components/';

const Login = () => {
    return (
        <Header>
            <LoginForm />
        </Header>
    );
};

export default Login;
